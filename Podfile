# Uncomment the next line to define a global platform for your project
platform :ios, '13.7'
use_frameworks!
inhibit_all_warnings!

target 'ChitChat02' do
  pod 'SwiftLint'
  pod 'Firebase/Firestore'
  
  target 'ChitChat02Tests' do
    inherit! :search_paths
  end
end
